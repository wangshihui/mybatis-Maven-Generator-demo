mvn gennerator 插件
构建命令
mvn mybatis-generator:generate 

maven配置
<build>
    <plugins>
            <plugin>
                <groupId>org.mybatis.generator</groupId>
                <artifactId>mybatis-generator-maven-plugin</artifactId>
                <version>1.3.2</version>
                <configuration>
                	默认使用 src\main\resources\generatorConfig.xml
                    <configurationFile>src/main/resources/generatorConfigTest.xml</configurationFile>
                    <verbose>true</verbose>
                    <overwrite>true</overwrite>
                    <tableNames>pubmDB1.lawsandregulations</tableNames>
                </configuration>
            </plugin>
        </plugins>
    </build>